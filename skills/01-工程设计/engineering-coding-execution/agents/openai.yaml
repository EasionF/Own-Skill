interface:
  display_name: "工程级编码执行规范"
  short_description: "按设计稳妥落地代码并验证。"
  default_prompt: "Use $engineering-coding-execution-guidelines to implement the confirmed design with bounded changes and verification."

policy:
  allow_implicit_invocation: true
