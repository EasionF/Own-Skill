interface:
  display_name: "Memory 读取规范"
  short_description: "读取历史上下文和经验。"
  default_prompt: "Use $memory-retrieval-guidelines to retrieve relevant memory context before continuing."

policy:
  allow_implicit_invocation: true
