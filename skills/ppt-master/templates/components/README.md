# SVG Component Template Library

Reusable non-page components for `ppt-master`, including image frames, callouts, and metric strips.

- Prefer `components_index.json` for lookup.
- Components are building blocks, not full slide templates.
- All SVGs follow the standard `viewBox="0 0 1280 720"` and placeholder rules.
